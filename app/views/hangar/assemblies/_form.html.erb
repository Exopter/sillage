<%= form_with model: [ :hangar, assembly ], class: "workspace-form" do |form| %>
  <% if assembly.errors.any? %><div class="workspace-errors"><%= assembly.errors.full_messages.to_sentence %></div><% end %>
  <div class="workspace-form-grid">
    <label><span>Exopter Asset ID</span><%= text_field_tag nil, assembly.internal_number || "Assigned automatically", disabled: true %></label>
    <label><span>Name</span><%= form.text_field :name, required: true, placeholder: "Flight data recorder assembly" %></label>
    <label class="is-wide"><span>Parent assembly</span><%= form.collection_select :parent_id, @parent_options, :id, ->(parent) { "#{parent.internal_number} · #{parent.name}" }, { include_blank: "No parent" } %></label>
  </div>
  <label><span>Notes</span><%= form.text_area :notes, rows: 4 %></label>
  <div class="workspace-actions">
    <%= link_to "Cancel", assembly.persisted? ? hangar_assembly_path(assembly) : hangar_assemblies_path, class: "button-secondary" %>
    <%= form.submit class: "button-primary" %>
  </div>
<% end %>
