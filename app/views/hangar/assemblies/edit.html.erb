<% content_for :title, "Edit #{@assembly.code}" %>
<%= render "hangar/tabs" %>
<div class="workspace-form-page">
  <header class="workspace-header"><div><span class="section-eyebrow">Assemblies</span><h2>Edit <%= @assembly.code %></h2><p>Update identity, hierarchy, and configuration notes.</p></div></header>
  <section class="workspace-panel workspace-form-panel"><%= render "form", assembly: @assembly %></section>
</div>
