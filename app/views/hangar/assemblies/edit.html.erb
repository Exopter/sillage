<% content_for :title, "Edit #{@assembly.internal_number}" %>
<%= render "hangar/tabs" %>
<div class="workspace-form-page">
  <header class="workspace-header"><div><span class="section-eyebrow">Assemblies</span><h2>Edit <%= @assembly.internal_number %></h2><p>Update the name, hierarchy, and configuration notes.</p></div></header>
  <section class="workspace-panel workspace-form-panel"><%= render "form", assembly: @assembly %></section>
</div>
