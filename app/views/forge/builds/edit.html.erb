<% content_for :title, "Edit #{@build.code}" %><%= render "forge/tabs" %>
<header class="workspace-header"><div><h1>Edit <%= @build.code %></h1><p>Saving also refreshes the hardware snapshot.</p></div></header>
<section class="workspace-panel"><%= render "form", build: @build %></section>
