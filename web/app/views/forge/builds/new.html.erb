<% content_for :title, "New build" %><%= render "forge/tabs" %>
<header class="workspace-header"><div><span class="section-eyebrow">OS Forge</span><h1>New build</h1><p>The current assembly composition is captured when this form is saved.</p></div></header>
<section class="workspace-panel"><%= render "form", build: @build %></section>
