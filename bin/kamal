#!/usr/bin/env ruby
# frozen_string_literal: true

ENV["BUNDLE_GEMFILE"] ||= File.expand_path("../Gemfile", __dir__)

require_relative "../config/environment_file"
Sillage::EnvironmentFile.load(File.expand_path("../.env.deploy.local", __dir__))

require "rubygems"
require "bundler/setup"

load Gem.bin_path("kamal", "kamal")
